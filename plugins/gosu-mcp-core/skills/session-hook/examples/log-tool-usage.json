{
  "description": "Log all tool usage to a file for debugging or auditing. Creates a JSON log at /tmp/claude-tool-log.jsonl",
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "tee -a /tmp/claude-tool-log.jsonl > /dev/null",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
